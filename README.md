## Personal site

### Development

```
$ hugo server --port <port> --bind <your_ip> --baseURL <your_ip> --disableFastRender
# e.g.: hugo server --port 8081 --bind localhost --baseURL localhost --disableFastRender
```

### Deploy

Just push your changes and Cloudflare will build
